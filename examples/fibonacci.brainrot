skibidi main{
    rizz first = 0;
    rizz second = 1;
    rizz next;  
    rizz count = 0;  
    rizz limit = 10;  

    yapping("%d", first);    
    yapping("%d", second);    
    count = count + 2;

    goon(count < limit){
        next = first + second;
        yapping("%d", next);

        first = second;
        second = next;
        
        count = count + 1;
    }

    bussin 0;
}