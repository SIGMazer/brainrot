skibidi main {
    cap nocap = W;

    edgy(nocap) {
        yapping("It's valid!");
    }
    
    nocap = W && L;
    yapping("nocap: ", nocap);
    nocap = W || L;
    yapping("nocap: ", nocap);
}
