skibidi main {
    cap nocap = yes;

    edgy(nocap) {
        yapping("It's valid!");
    }
    
    nocap = yes && no;
    yapping("nocap: ", nocap);
    nocap = yes || no;
    yapping("nocap: ", nocap);
}