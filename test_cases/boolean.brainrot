skibidi main {
    cap nocap = W;

    edgy(nocap) {
        yapping("It's valid!");
    }
    
    nocap = W && L;
    yapping("nocap: %b", nocap); 🚽 use %b identifier for boolean types
    nocap = W || L;
    yapping("nocap: %b", nocap);
}
